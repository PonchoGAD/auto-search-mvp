# Auto Search MVP — VPS Runbook

## Требования
- Ubuntu 20.04+
- Docker >= 24
- Docker Compose v2
- Минимум: 2 CPU / 4 GB RAM

## Установка Docker
```bash
curl -fsSL https://get.docker.com | bash
sudo usermod -aG docker $USER
